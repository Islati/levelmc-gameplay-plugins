package com.levelmc.skreet.gadgets.phone;

import com.levelmc.core.api.gui.ItemMenu;
import com.levelmc.skreet.users.SkreetPlayer;

public class PhoneMenu extends ItemMenu {
    public PhoneMenu(SkreetPlayer user) {
        super("&bCell Phone &7> &bMain Menu", 4);

        //Todo Implement:
        /*
        - Show reputation in top left (on coin block)

        - Quests menu shortcut
        - Lands menu shortcut
        - Gang menu shortcut
        - Tag manager shortcut
        - Skills menu shortcut
        - Store menu shortcut (to craft donate store)
         */
    }
}
